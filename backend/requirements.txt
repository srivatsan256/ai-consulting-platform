# Django Core
django>=4.2,<5.0
djangorestframework>=3.14
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
django-filter>=23.5
drf-spectacular>=0.27
django-extensions>=3.2
whitenoise>=6.6
gunicorn>=21.2
dj-database-url>=2.1
psycopg2-binary>=2.9

# AI / LLM
google-genai>=1.0
langchain>=0.1
langchain-community>=0.0.10
langchain-text-splitters>=0.0.1

# AirLLM (local LLM inference via layer-wise offloading).
# Optional: needed only for AI_PROVIDER=airllm. The service falls back to
# the mock client when this is not installed.
# airllm>=2.11
# torch>=2.2  # dependency of airllm (CPU/GPU), heavy install

# Vector Database
chromadb>=0.4
sentence-transformers>=2.3

# Document Processing
PyPDF2>=3.0.1
python-docx>=1.1
openpyxl>=3.1

# Utilities
python-dotenv>=1.0
tiktoken>=0.5
numpy>=1.26

